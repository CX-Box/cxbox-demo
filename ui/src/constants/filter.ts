import { FieldType } from '@cxbox-ui/schema'
import { CustomFieldTypes } from '@interfaces/widget'

export const checkboxFilterMaxVisibleItems = 7
export const checkboxFilterCounterLimit = 9

export const checkboxFilterFieldTypes = [FieldType.dictionary, CustomFieldTypes.MultipleSelect, FieldType.radio]
